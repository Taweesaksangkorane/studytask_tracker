rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Users can only access files under their own UID path.
    match /users/{userId}/tasks/{taskId}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
